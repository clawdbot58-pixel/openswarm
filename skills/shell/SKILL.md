# Shell Skill

Basic shell command execution for agents.

## Commands

- `run <command>` - Execute shell command
- `background <command>` - Run in background
- `kill <pid>` - Kill process

## Safety

- Commands run in workspace context
- Timeout limits enforced
- No destructive commands without confirmation
